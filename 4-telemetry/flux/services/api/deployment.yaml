apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: filhodanuvem-services
  labels:
    app: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: cloudson/log-api:latest
        ports:
        - containerPort: 8081
